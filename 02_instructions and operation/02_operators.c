#include<stdio.h>

int main()
{
    int a = 4;
    int b = 3;
    int c=a+b;
    printf("additions %d", c);

    // modulus operators is used to get remainder
    printf("the remainder when a is divided by b is : %d\n", a%b);

    // int d = a^b will not work in c  
}

